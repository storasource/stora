# Vercel Blob Storage (required for cloud deployment)
# Screenshots are uploaded to Vercel Blob for persistent storage
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_XXXXXXXXXX

# Setup Instructions:
# 1. Go to https://vercel.com/dashboard/stores
# 2. Create a new Blob store (or use existing)
# 3. Copy the Read/Write token
# 4. Set this environment variable on your Scaleway runner:
#    export BLOB_READ_WRITE_TOKEN=your_actual_token_here
#
# Fallback Behavior:
# If BLOB_READ_WRITE_TOKEN is not set, screenshots will be saved locally
# to stora-platform/public/videos/ (suitable for development only)
